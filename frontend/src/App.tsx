import {useState} from 'react';import {ask} from './lib/api';import type {QueryResponse} from './types';import {ChartPanel} from './components/ChartPanel';import {EvidencePanel} from './components/EvidencePanel';import './style.css'
const examples=['最近四个月毛利润趋势','最近三个月利润为什么下降','华东区域的收入和成本是多少']
export default function App(){const [question,setQuestion]=useState(examples[1]);const [result,setResult]=useState<QueryResponse|null>(null);const [loading,setLoading]=useState(false);const [error,setError]=useState('');async function submit(){setLoading(true);setError('');try{setResult(await ask(question))}catch(e){setError(e instanceof Error?e.message:'请求失败')}finally{setLoading(false)}}return <main><header><span className="eyebrow">MANUFACTURING INTELLIGENCE</span><h1>InsightBI Agent</h1><p>从自然语言问题到可审计的数据结论</p></header><section className="ask"><textarea value={question} onChange={e=>setQuestion(e.target.value)} rows={3}/><button onClick={submit} disabled={loading||!question.trim()}>{loading?'分析中…':'开始分析'}</button><div className="examples">{examples.map(x=><button key={x} onClick={()=>setQuestion(x)}>{x}</button>)}</div></section>{error&&<p className="error">{error}</p>}{result&&<><section className="result"><div><span className="badge">{result.mode==='analysis'?'复杂归因':'简单查数'}</span><span className="meta">{result.latency_ms} ms · {result.trace_id}</span></div><h2>分析结论</h2><p>{result.answer}</p></section><div className="grid">{result.charts.map((c,i)=><ChartPanel key={i} chart={c}/>)}</div><EvidencePanel items={result.evidence}/></>}</main>}

